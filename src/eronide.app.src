%% ERlang ONline IDE.

{application, eronide, [
	{description, "ERlang ONline IDE."},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [l_sup]},
	{applications, [
		kernel,
		stdlib,
		crypto,
		public_key,
		ssl,
		cowboy
	]},
	{mod, {eronide, []}},
	{env, []}
]}.
